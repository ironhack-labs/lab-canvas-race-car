function generateRivals() {
  let img, rndom;
  if (frames % 100)
         = 0;
  rndom = Math.random() * canvas.width;
  if (Math.random >= .5)
    CO.push(new Rival(rival => Rival.draw(0, rndom)));
}
