class Obstacle {
    constructor (ctx) {
        this.ctx = ctx;

        this.dist = Math.random() 
        this.x = Math.random() > 

        this.w = this.ctx.canvas.width;
        this.h = Math.random() * 40 + 50;
    }
}