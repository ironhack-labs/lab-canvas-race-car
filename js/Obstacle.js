/*
class Obstacle extends Component {
    constructor(classGame, x, y, width, height, color){
        super(classGame, x, y, width, height);

        this.color = color;
    }

    makeObstacle(){
     
        fillStyle = this.color;
        fillRect(this.x, this.y, this.width, this.height)
    }


}

*/