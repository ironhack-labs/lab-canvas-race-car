window.onload = () => {
  document.getElementById('start-button').onclick = () => {
    startGame();
  };
  

  raceGame.init('canvas')
  function startGame() {
    //raceGame.init('canvas') //poner esta y quitar la de arriba cuando para que cuando se apriete el boon se inicie el juego
  }
   
};
